// GitHub Pages cannot call http://127.0.0.1:3000 (Chrome blocks it, especially in Incognito).
// 1) Deploy folder /api to Render (free): https://render.com → New Web Service → rootDir: api
// 2) Paste your public URL below (https only)
// 3) Run: npm run deploy
window.APP_CONFIG = { apiUrl: '' };
