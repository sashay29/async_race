// Deploy async-race-api (see /api folder + README) and paste your public API URL here.
// Example: window.APP_CONFIG = { apiUrl: 'https://async-race-api-xxxx.onrender.com' };
window.APP_CONFIG = window.APP_CONFIG || { apiUrl: '' };
